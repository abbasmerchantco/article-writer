#!/usr/bin/env bash
# DEPRECATED — removed in the article-writer simplification (see CHANGELOG.md).
# The plugin now only emits Markdown (article.md with site frontmatter) — no .docx
# output path exists anymore. Nothing in this plugin calls this script. Safe to delete.
echo "to-docx.sh: deprecated, no longer used by article-writer (see CHANGELOG.md)" >&2
exit 0
