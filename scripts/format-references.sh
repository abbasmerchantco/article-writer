#!/usr/bin/env bash
# DEPRECATED — removed in the article-writer simplification (see CHANGELOG.md).
# The citation-style menu and formal bibliography rendering this script performed no
# longer exist — sources, when relevant, are just linked/mentioned naturally in the
# article body. Nothing in this plugin calls this script anymore. Safe to delete.
echo "format-references.sh: deprecated, no longer used by article-writer (see CHANGELOG.md)" >&2
exit 0
