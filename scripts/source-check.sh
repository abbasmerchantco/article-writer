#!/usr/bin/env bash
# DEPRECATED — removed in the article-writer simplification (see CHANGELOG.md).
# The whitelist/blacklist/tiering enforcement this script performed no longer exists —
# fact-checking is now a matter of the model's own judgement during drafting, not a
# deterministic gate. Nothing in this plugin calls this script anymore. Safe to delete.
echo "source-check.sh: deprecated, no longer used by article-writer (see CHANGELOG.md)" >&2
exit 0
