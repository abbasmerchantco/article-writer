#!/usr/bin/env bash
# DEPRECATED — removed in the article-writer simplification (see CHANGELOG.md).
# The per-gate cap/escalation system this script enforced no longer exists. Nothing in
# this plugin calls this script anymore. Safe to delete this file.
echo "gate-counter.sh: deprecated, no longer used by article-writer (see CHANGELOG.md)" >&2
exit 0
