#!/usr/bin/env bash
# DEPRECATED — removed in the article-writer simplification (see CHANGELOG.md).
# The Step 8 adversarial-review loop this script controlled no longer exists. Nothing in
# this plugin calls this script anymore. Safe to delete this file.
echo "review-loop.sh: deprecated, no longer used by article-writer (see CHANGELOG.md)" >&2
exit 0
