{
  "$comment": "DEPRECATED — removed in the article-writer simplification (see CHANGELOG.md). The whitelist/blacklist/tiering enforcement this file backed no longer exists; source judgement is now up to the model during drafting, guided by plain instructions in commands/write-article.md rather than an enforced policy file. Safe to delete this file."
}
