#!/usr/bin/env bash
# DEPRECATED — tested gate-guard.sh's PreToolUse hook, removed (hooks/hooks.json no
# longer registers it — see CHANGELOG.md). Safe to delete this file.
echo "phase2-guard.sh: deprecated, the guard hook was removed (see CHANGELOG.md)"
exit 0
