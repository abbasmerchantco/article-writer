#!/usr/bin/env bash
# DEPRECATED — tested gate-counter.sh and the old input/interim/output layout, both
# removed (see CHANGELOG.md). Replaced by tests/smoke.sh. Safe to delete this file.
echo "phase1-core.sh: deprecated, superseded by tests/smoke.sh (see CHANGELOG.md)"
exit 0
