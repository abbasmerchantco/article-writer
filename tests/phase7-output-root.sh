#!/usr/bin/env bash
# DEPRECATED — tested the old input/interim/output layout under output_root; replaced
# by the flat single-folder layout (see CHANGELOG.md). output_root itself is still
# covered by tests/smoke.sh implicitly (AW_OUTPUT_ROOT / cwd resolution in init-run.sh
# is unchanged). Safe to delete this file.
echo "phase7-output-root.sh: deprecated, superseded by tests/smoke.sh (see CHANGELOG.md)"
exit 0
