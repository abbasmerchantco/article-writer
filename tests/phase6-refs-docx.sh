#!/usr/bin/env bash
# DEPRECATED — tested format-references.sh and to-docx.sh, both removed; there is no
# citation-style menu or docx output anymore (see CHANGELOG.md). Safe to delete.
echo "phase6-refs-docx.sh: deprecated, citations/docx were removed (see CHANGELOG.md)"
exit 0
